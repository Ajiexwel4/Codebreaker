module Codebreaker
  RSpec.describe Score do
    # After the game is won or lost, the code-breaker can opt to save information about the game: who (initials?), how many turns, and so on.   
    xcontext "Code-breaker saves score"
  end
end